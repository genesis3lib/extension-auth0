# Auth0 Secrets Configuration
# These should be stored in a secrets manager (AWS Secrets Manager, Vault, etc.)
#
# IMPORTANT: Do NOT commit actual secret values to version control
#
# For local development, create a .env file with these values

# Auth0 Client Secret
# Required for backend authentication
AUTH0_CLIENT_SECRET: ${AUTH0_CLIENT_SECRET}
